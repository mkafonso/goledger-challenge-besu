version: "2"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "db"
        out: "./sqlc"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_exact_table_names: false
